node_modules
dist
dist-ssr
coverage
*.local
.DS_Store
.vite
playwright-report
test-results

# scratch: throwaway scripts and probes. Kept OUT of src/ so vitest's include glob
# ("src/**/*.test.{ts,tsx}") can never pick up a file CI does not see.
scratch/
.npm-cache/
public/ocr/
